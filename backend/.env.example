# SwiftCart Order Manager - Environment Variables Template
# Copy this file to .env and replace with your actual values

# Database Configuration
# MongoDB Atlas connection string (replace with your actual URI)
MONGO_URL=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/

# Database name
DB_NAME=swiftcart_orders

# CORS Settings (for production)
# Comma-separated list of allowed origins
CORS_ORIGINS=https://yourdomain.com,http://localhost:3000

# Application Settings
# Backend URL for load testing and CORS
BACKEND_URL=https://your-api-domain.com

# Environment (development, staging, production)
ENVIRONMENT=development

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: JWT Secret for authentication (if implementing auth)
# JWT_SECRET=your-super-secret-jwt-key-here

# Optional: Email configuration (for notifications)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Optional: Redis URL (for caching)
# REDIS_URL=redis://localhost:6379

# Note: Never commit your actual .env file to version control!
# Add .env to your .gitignore file
